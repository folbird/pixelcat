# 📦 像素猫 (Desktop Pet) — 跨平台打包指南

> 文档版本: 1.0  
> 生成日期: 2026-08-14  
> 项目类型: Tauri v2 桌面应用  
> 适用平台: macOS / Windows

---

## 目录

- [一、打包总览](#一打包总览)
- [二、macOS 打包指南](#二macos-打包指南)
- [三、Windows 打包指南（需先适配）](#三windows-打包指南需先适配)
- [四、Windows 代码适配改造](#四windows-代码适配改造)
- [五、GitHub Actions 自动打包（推荐）](#五github-actions-自动打包推荐)
- [六、常见问题 FAQ](#六常见问题-faq)

---

## 一、打包总览

| 平台 | 当前状态 | 打包产物 | 说明 |
|------|---------|---------|------|
| **macOS** | ✅ 可直接打包 | `.dmg` / `.app` | 当前代码原生支持 |
| **Windows** | ⚠️ 需先适配 | `.exe` (NSIS/MSI) | 需改造 macOS 专属依赖 |

**核心结论**：项目基于 Tauri 2，架构本身跨平台。但 Rust 后端深度依赖 macOS 专属 API（`tauri-nspanel`、`objc2` 系列），Windows 打包前必须先做条件编译适配（详见第四章）。

---

## 二、macOS 打包指南

### 2.1 环境要求

| 工具 | 版本要求 | 检查命令 |
|------|---------|---------|
| macOS | 10.15+ | `sw_vers` |
| Node.js | ≥ 18 | `node --version` |
| Rust | ≥ 1.70 | `rustc --version` |
| Xcode Command Line Tools | 最新 | `xcode-select -p` |
| Tauri CLI | ^2.11.3 | `npx tauri --version` |

### 2.2 安装依赖

```bash
# 1. 安装 Node.js 依赖
npm install

# 2. 安装 Rust 工具链（如未安装）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 3. 安装 Xcode Command Line Tools（如未安装）
xcode-select --install
```

### 2.3 开发模式运行

```bash
# 推荐：带稳定签名（全局键盘监听权限可复用）
npm run dev:signed

# 或：直接 Tauri 开发模式
npm run tauri dev
```

### 2.4 构建发布版本

```bash
npm run tauri build
```

**构建产物位置**：
```
src-tauri/target/release/bundle/
├── dmg/
│   └── desktop-pet_0.1.0_aarch64.dmg    # Apple Silicon
│   └── desktop-pet_0.1.0_x64.dmg        # Intel
├── macos/
│   └── desktop-pet.app                  # 未打包的 .app
└── msi/                                 # (如配置)
```

### 2.5 构建指定架构

```bash
# Apple Silicon (M1/M2/M3)
npm run tauri build -- --target aarch64-apple-darwin

# Intel Mac
npm run tauri build -- --target x86_64-apple-darwin

# 通用二进制（同时支持两种架构）
npm run tauri build -- --target universal-apple-darwin
```

> 交叉编译需先安装对应 target：`rustup target add aarch64-apple-darwin x86_64-apple-darwin`

### 2.6 代码签名与公证（发布到 App Store 外）

**注意**：未签名/未公证的 `.dmg` 在他人 Mac 上打开会提示「无法验证开发者」。

#### 步骤 1：配置签名证书

在 `src-tauri/tauri.conf.json` 中添加：

```json
{
  "bundle": {
    "active": true,
    "targets": "all",
    "macOS": {
      "signingIdentity": "Developer ID Application: Your Name (TEAMID)",
      "providerShortName": "TEAMID"
    }
  }
}
```

#### 步骤 2：配置公证（Notarization）

在 `src-tauri/tauri.conf.json` 中添加：

```json
{
  "bundle": {
    "macOS": {
      "entitlements": null,
      "signingIdentity": "Developer ID Application: Your Name (TEAMID)",
      "providerShortName": "TEAMID"
    }
  }
}
```

设置环境变量（Apple ID 专用密码）：

```bash
export APPLE_ID="your@email.com"
export APPLE_PASSWORD="xxxx-xxxx-xxxx-xxxx"
export APPLE_TEAM_ID="TEAMID"
npm run tauri build
```

#### 步骤 3：手动签名（无证书时本地自用）

```bash
# 对构建出的 .app 进行 ad-hoc 签名
codesign --force --deep --sign - src-tauri/target/release/bundle/macos/desktop-pet.app
```

### 2.7 macOS 权限设置（运行时）

`keytap` 全局键盘监听需要辅助功能权限：

1. 打开 **系统设置 → 隐私与安全性 → 辅助功能**
2. 点击 `+` 添加应用（`src-tauri/target/release/bundle/macos/desktop-pet.app`）
3. 勾选启用

> 开发模式下应用位于 `src-tauri/target/debug/`，发布版位于 `src-tauri/target/release/`。

### 2.8 macOS 打包检查清单

- [ ] `npm install` 成功
- [ ] `npm run tauri build` 无报错
- [ ] 产物 `.dmg` 生成于 `src-tauri/target/release/bundle/dmg/`
- [ ] 双击 `.dmg` 安装后应用能正常启动
- [ ] 全局键盘监听已授权（辅助功能权限）
- [ ] 开机自启功能正常（LaunchAgent）
- [ ] 全屏覆盖正常（NSPanel）

---

## 三、Windows 打包指南（需先适配）

### 3.1 环境要求

| 工具 | 版本要求 | 说明 |
|------|---------|------|
| Windows | 10/11 (64-bit) | 推荐 Windows 11 |
| Node.js | ≥ 18 | 从 [nodejs.org](https://nodejs.org) 下载 |
| Rust | ≥ 1.70 | 从 [rustup.rs](https://rustup.rs) 安装 |
| Microsoft C++ Build Tools | 最新 | 需含 "Desktop development with C++" 工作负载 |
| WebView2 Runtime | 内置 | Windows 11 自带，Win10 需安装 |
| Tauri CLI | ^2.11.3 | `npm install` 后自动安装 |

### 3.2 安装 Windows 构建工具

```powershell
# 1. 安装 Rust（如未安装）
# 下载 https://win.rustup.rs/x86_64 并运行

# 2. 安装 Microsoft C++ Build Tools
# 下载 https://visualstudio.microsoft.com/visual-cpp-build-tools/
# 勾选 "Desktop development with C++" 工作负载
# 包含：MSVC v143、Windows 10/11 SDK、CMake

# 3. 验证环境
node --version    # ≥ 18
rustc --version   # ≥ 1.70
cargo --version
```

### 3.3 重要：先完成代码适配

**⚠️ 在 Windows 上执行 `npm run tauri build` 之前，必须先完成第四章的代码适配改造。** 否则编译会因 macOS 专属依赖（`tauri-nspanel`、`objc2`）而失败。

### 3.4 安装依赖并构建

```powershell
# 1. 安装 Node.js 依赖
npm install

# 2. 开发模式运行（适配后）
npm run tauri dev

# 3. 构建发布版本
npm run tauri build
```

### 3.5 构建产物

```
src-tauri/target/release/bundle/
├── nsis/
│   └── desktop-pet_0.1.0_x64-setup.exe    # NSIS 安装包（推荐分发）
└── msi/
    └── desktop-pet_0.1.0_x64.msi          # MSI 安装包
```

### 3.6 构建指定架构

```powershell
# x64（默认）
npm run tauri build

# ARM64
npm run tauri build -- --target aarch64-pc-windows-msvc

# 需先安装 target
rustup target add x86_64-pc-windows-msvc
rustup target add aarch64-pc-windows-msvc
```

### 3.7 Windows 打包检查清单

- [ ] 完成第四章代码适配
- [ ] `npm install` 成功
- [ ] `npm run tauri build` 无报错
- [ ] 产物 `.exe` 生成于 `src-tauri/target/release/bundle/nsis/`
- [ ] 双击安装包安装后应用能正常启动
- [ ] 全局键盘监听正常（Windows 无需额外权限）
- [ ] 开机自启功能正常（注册表方式）
- [ ] 透明窗口正常显示
- [ ] 置顶功能正常

---

## 四、Windows 代码适配改造

> 本节是 Windows 打包的**前置条件**。改造后不影响 macOS 功能。

### 4.1 修改 `src-tauri/Cargo.toml`

将 macOS 专属依赖移到 `[target.'cfg(target_os = "macos")'.dependencies]`：

```toml
[package]
name = "tauri-app"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

[lib]
name = "tauri_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
keytap = "0.4.0"

# ===== macOS 专属依赖（Windows 上不编译）=====
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = { version = "0.6.4", features = ["objc2-proc-macros"] }
objc2-foundation = "0.3.2"
objc2-app-kit = "0.3.2"
tauri-nspanel = { git = "https://github.com/ahkohd/tauri-nspanel", branch = "v2.1" }

# ===== Windows 专属依赖 =====
[target.'cfg(windows)'.dependencies]
winreg = "0.52"          # 开机自启（注册表）
windows-sys = { version = "0.59", features = [
    "Win32_UI_WindowsAndMessaging",
    "Win32_Foundation",
    "Win32_Graphics_Gdi",
] }
```

**关键改动说明**：
- 移除 `tauri` 的 `macos-private-api` feature（仅 macOS 需要，Windows 编译会报错）
- `keytap` 保留在通用依赖（它同时支持 macOS/Windows/Linux）
- 新增 `winreg` 用于 Windows 开机自启
- 新增 `windows-sys` 用于 Windows 光标位置获取（果冻拖拽）

### 4.2 修改 `src-tauri/src/lib.rs`

#### 改动 1：条件导入 macOS 专属模块

```rust
use keytap::{EventKind, Tap};
#[cfg(target_os = "macos")]
use std::ffi::c_void;
#[cfg(target_os = "macos")]
use std::ptr;
#[cfg(target_os = "macos")]
use std::sync::Mutex;
use std::time::Duration;
use tauri::menu::CheckMenuItem;
use tauri::menu::{Menu, MenuEvent, MenuItem, PredefinedMenuItem};
use tauri::tray::{MouseButton, MouseButtonState, TrayIconBuilder, TrayIconEvent};
use tauri::{Emitter, LogicalSize, Manager, Size, WebviewWindow};

// macOS 专属：NSPanel 相关
#[cfg(target_os = "macos")]
use tauri_nspanel::{tauri_panel, PanelLevel, StyleMask, WebviewWindowExt};
```

#### 改动 2：条件编译 `tauri_panel!` 宏

```rust
#[cfg(target_os = "macos")]
tauri_panel! {
    panel!(BasicPanel {
        config: {
            can_become_key_window: true,
            is_floating_panel: true
        }
    })
}
```

#### 改动 3：条件注册 NSPanel 插件

```rust
pub fn run() {
    let mut builder = tauri::Builder::default();

    // macOS 专属：NSPanel 插件
    #[cfg(target_os = "macos")]
    {
        builder = builder.plugin(tauri_nspanel::init());
    }

    builder
        .manage(AppWindow(Mutex::new(None)))
        // ... 其余保持不变
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

#### 改动 4：Windows 开机自启实现

替换现有的 `write_launch_agent` 非 macOS fallback：

```rust
// ============================================================
// 开机自启
// ============================================================

#[cfg(target_os = "macos")]
fn launch_agent_path() -> std::path::PathBuf {
    // ... 保持原有 macOS 实现不变
}

#[cfg(target_os = "macos")]
fn write_launch_agent(enabled: bool) -> std::io::Result<()> {
    // ... 保持原有 macOS 实现不变
}

#[cfg(target_os = "macos")]
fn startup_enabled() -> bool {
    launch_agent_path().exists()
}

// ===== Windows 开机自启（注册表）=====
#[cfg(windows)]
fn write_launch_agent(enabled: bool) -> std::io::Result<()> {
    use winreg::enums::HKEY_CURRENT_USER;
    use winreg::RegKey;

    const RUN_KEY: &str = r"Software\Microsoft\Windows\CurrentVersion\Run";
    const APP_NAME: &str = "desktop-pet";

    let hkcu = RegKey::predef(HKEY_CURRENT_USER);
    let (key, _) = hkcu.create_subkey(RUN_KEY)?;

    if enabled {
        let exe = std::env::current_exe()?;
        key.set_value(APP_NAME, &exe.to_string_lossy().to_string())?;
    } else {
        let _ = key.delete_value(APP_NAME);
    }
    Ok(())
}

#[cfg(windows)]
fn startup_enabled() -> bool {
    use winreg::enums::HKEY_CURRENT_USER;
    use winreg::RegKey;

    const RUN_KEY: &str = r"Software\Microsoft\Windows\CurrentVersion\Run";
    const APP_NAME: &str = "desktop-pet";

    let hkcu = RegKey::predef(HKEY_CURRENT_USER);
    if let Ok(key) = hkcu.open_subkey(RUN_KEY) {
        return key.get_value::<String, _>(APP_NAME).is_ok();
    }
    false
}

// ===== Linux 或其他平台 fallback =====
#[cfg(not(any(target_os = "macos", windows)))]
fn startup_enabled() -> bool {
    false
}

#[cfg(not(any(target_os = "macos", windows)))]
fn write_launch_agent(_enabled: bool) -> std::io::Result<()> {
    Ok(())
}
```

#### 改动 5：Windows 光标位置获取（果冻拖拽）

`start_refresh_loop` 目前是 macOS 专属（用 CoreGraphics）。为 Windows 添加等效实现：

```rust
// ===== Windows 光标位置轮询（果冻拖拽）=====
#[cfg(windows)]
fn start_refresh_loop(app_handle: tauri::AppHandle) {
    use windows_sys::Win32::Foundation::POINT;
    use windows_sys::Win32::UI::WindowsAndMessaging::{GetAsyncKeyState, GetCursorPos};

    std::thread::spawn(move || {
        std::thread::sleep(Duration::from_millis(500));
        let mut refresh_counter = 0u8;
        loop {
            std::thread::sleep(Duration::from_millis(16));
            refresh_counter = refresh_counter.wrapping_add(1);
            let h = app_handle.clone();
            let _ = app_handle.run_on_main_thread(move || {
                if let Some(window) = h.get_webview_window("main") {
                    let mut point = POINT { x: 0, y: 0 };
                    unsafe { GetCursorPos(&mut point) };
                    let left_down = unsafe { GetAsyncKeyState(0x01) } & 0x8000 != 0;

                    if let (Ok(position), Ok(size)) = (window.outer_position(), window.outer_size()) {
                        let center_x = position.x as f64 + size.width as f64 / 2.0;
                        let center_y = position.y as f64 + size.height as f64 / 2.0;
                        let _ = h.emit(
                            "cursor-position",
                            serde_json::json!({
                                "x": point.x as f64,
                                "y": point.y as f64,
                                "dx": point.x as f64 - center_x,
                                "dy": point.y as f64 - center_y,
                                "down": left_down
                            }),
                        );
                    }
                }
            });
        }
    });
}
```

#### 改动 6：Windows 全屏覆盖

macOS 用 NSPanel 实现全屏覆盖。Windows 上透明置顶窗口天然支持全屏覆盖（`alwaysOnTop: true` 已配置），无需额外代码。但可增强：

```rust
// ===== Windows 置顶增强（可选）=====
#[cfg(windows)]
fn make_panel_float_on_top(window: &tauri::WebviewWindow) {
    // Windows 上 Tauri 的 set_always_on_top 已足够
    let _ = window.set_always_on_top(true);
}
```

#### 改动 7：`show_water_log` / `hide_water_log` 的 Windows 处理

当前这两个命令在非 macOS 下返回 `Err("panel not ready")`。改为 Windows 上直接显示/隐藏窗口：

```rust
#[tauri::command]
fn show_water_log(app: tauri::AppHandle) -> Result<(), String> {
    #[cfg(target_os = "macos")]
    {
        if let Some(window) = app.get_webview_window("water-log") {
            if let Ok(panel) = window.to_panel::<BasicPanel>() {
                panel.show_and_make_key();
                return Ok(());
            }
        }
    }
    // Windows/Linux：直接显示窗口
    #[cfg(not(target_os = "macos"))]
    {
        if let Some(window) = app.get_webview_window("water-log") {
            let _ = window.show();
            let _ = window.set_focus();
            return Ok(());
        }
    }
    Err("panel not ready".into())
}

#[tauri::command]
fn hide_water_log(app: tauri::AppHandle) -> Result<(), String> {
    #[cfg(target_os = "macos")]
    {
        if let Some(window) = app.get_webview_window("water-log") {
            if let Ok(panel) = window.to_panel::<BasicPanel>() {
                panel.hide();
            }
        }
    }
    // Windows/Linux：直接隐藏窗口
    #[cfg(not(target_os = "macos"))]
    {
        if let Some(window) = app.get_webview_window("water-log") {
            let _ = window.hide();
        }
    }
    Ok(())
}
```

#### 改动 8：`make_panel_key` / `keep_on_top` 的 Windows 处理

```rust
#[tauri::command]
fn keep_on_top(state: tauri::State<'_, AppWindow>) {
    #[cfg(target_os = "macos")]
    if let Some(ref window) = *state.0.lock().unwrap() {
        make_panel_float_on_top(window);
    }
    // Windows：无需额外处理，alwaysOnTop 已生效
}

#[tauri::command]
fn make_panel_key(state: tauri::State<'_, AppWindow>) {
    #[cfg(target_os = "macos")]
    if let Some(ref window) = *state.0.lock().unwrap() {
        if let Ok(panel) = window.to_panel::<BasicPanel>() {
            panel.make_key_window();
        }
    }
    // Windows：无需额外处理
}
```

### 4.3 修改 `src-tauri/tauri.conf.json`

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "desktop-pet",
  "version": "0.1.0",
  "identifier": "com.jun.desktop-pet",
  "build": {
    "frontendDist": "../src",
    "beforeDevCommand": "",
    "beforeBuildCommand": ""
  },
  "app": {
    "withGlobalTauri": true,
    "windows": [
      {
        "label": "main",
        "title": "",
        "width": 128,
        "height": 128,
        "resizable": false,
        "decorations": false,
        "transparent": true,
        "shadow": false,
        "alwaysOnTop": true,
        "skipTaskbar": true,
        "fullscreen": false,
        "center": true
      },
      {
        "label": "water-log",
        "title": "",
        "url": "water-log.html",
        "width": 520,
        "height": 360,
        "resizable": false,
        "decorations": false,
        "transparent": true,
        "shadow": false,
        "alwaysOnTop": true,
        "skipTaskbar": true,
        "visible": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "windows": {
      "nsis": {
        "installMode": "currentUser",
        "displayLanguageSelector": true
      }
    }
  }
}
```

**关键改动**：
- 移除 `"macOSPrivateApi": true`（仅 macOS 需要）
- 新增 `bundle.windows.nsis` 配置（NSIS 安装包选项）

### 4.4 前端代码检查

前端 `src/main.js` 和 `src/water-log.js` 使用 `window.__TAURI__` 标准 API，**无需修改**。已确认：
- ✅ 无平台相关代码（`navigator.platform` 等）
- ✅ 使用标准 `invoke()` / `listen()` API
- ✅ 事件监听有兜底处理（`T && T.core && T.core.invoke` 判断）

### 4.5 适配后验证

```bash
# macOS 上验证（确保适配没破坏 macOS 功能）
npm run tauri build

# Windows 上验证（需在 Windows 机器或 CI 上）
npm run tauri build
```

---

## 五、GitHub Actions 自动打包（推荐）

无需本地 Windows 机器，用 GitHub Actions 自动构建 Windows 和 macOS 安装包。

### 5.1 创建 `.github/workflows/build.yml`

```yaml
name: Build Desktop Pet

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, windows-latest]

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri'

      - name: Install Linux dependencies (if needed)
        if: matrix.platform == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        run: npm install

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: '像素猫 v__VERSION__'
          releaseBody: '自动构建的发布版本'
          releaseDraft: true
          prerelease: false
```

### 5.2 触发构建

```bash
# 打 tag 推送即可触发
git tag v0.1.0
git push origin v0.1.0
```

构建完成后，GitHub Release 会自动生成：
- `desktop-pet_0.1.0_x64.dmg`（macOS）
- `desktop-pet_0.1.0_x64-setup.exe`（Windows）

---

## 六、常见问题 FAQ

### Q1: Windows 编译报错 `tauri-nspanel` 找不到？

**原因**：`tauri-nspanel` 是 macOS 专属 crate，Windows 上无法编译。
**解决**：按第四章 4.1 节，将 `tauri-nspanel` 和 `objc2*` 移到 `[target.'cfg(target_os = "macos")'.dependencies]`。

### Q2: Windows 编译报错 `macos-private-api` feature 不存在？

**原因**：`macos-private-api` 是 Tauri 的 macOS 专属 feature。
**解决**：从 `Cargo.toml` 的 `tauri` 依赖中移除该 feature。

### Q3: Windows 上全局键盘监听不工作？

**原因**：`keytap` 在 Windows 上需要管理员权限或特定配置。
**解决**：
- 确认 `keytap` 版本支持 Windows（0.4.0 支持，依赖 `windows-sys`）
- 尝试以管理员身份运行应用
- 检查 Windows 是否有输入法/安全软件拦截

### Q4: Windows 上透明窗口显示黑色背景？

**原因**：Windows 透明窗口需要 WebView2 正确配置。
**解决**：
- 确认 `transparent: true` 已配置
- 确认 WebView2 Runtime 已安装（Windows 11 自带）
- 尝试更新 WebView2 Runtime

### Q5: macOS 打包后提示「无法验证开发者」？

**原因**：应用未签名或未公证。
**解决**：
- 本地自用：`codesign --force --deep --sign - <app路径>`
- 分发他人：配置 Developer ID 证书 + 公证（见 2.6 节）

### Q6: 如何同时构建 macOS 和 Windows 版本？

**推荐方案**：使用 GitHub Actions（见第五章），在 CI 上分别构建两个平台。
**本地方案**：macOS 上只能构建 macOS 包；Windows 上只能构建 Windows 包（Tauri 不支持跨平台交叉编译）。

### Q7: 打包体积太大？

**原因**：Tauri 打包包含 WebView2 运行时（Windows）或 WebKit（macOS）。
**解决**：
- Windows：NSIS 安装包默认不包含 WebView2（系统已装），体积较小
- macOS：`.dmg` 通常 5-10MB
- 可配置 `bundle.webviewInstallMode` 控制 WebView2 安装方式

### Q8: 开机自启在 Windows 上不生效？

**原因**：适配前 `write_launch_agent` 在 Windows 上是空实现。
**解决**：按第四章 4.2 节改动 4，实现 Windows 注册表版开机自启。

### Q9: 果冻拖拽形变在 Windows 上不生效？

**原因**：`start_refresh_loop` 使用 macOS CoreGraphics API。
**解决**：按第四章 4.2 节改动 5，实现 Windows 版 `GetCursorPos` + `GetAsyncKeyState`。

### Q10: 全屏看视频时小猫不显示（Windows）？

**原因**：Windows 全屏应用（如视频播放器）可能独占屏幕。
**解决**：
- 确认 `alwaysOnTop: true` 已配置
- 部分全屏应用（如游戏）使用独占全屏模式，无法覆盖
- 可尝试 `window.set_always_on_top(true)` 在运行时重新应用

---

## 附录 A：打包命令速查

```bash
# macOS
npm run tauri build                          # 构建当前架构
npm run tauri build -- --target aarch64-apple-darwin   # Apple Silicon
npm run tauri build -- --target x86_64-apple-darwin    # Intel
npm run tauri build -- --target universal-apple-darwin # 通用

# Windows
npm run tauri build                          # 构建 x64
npm run tauri build -- --target aarch64-pc-windows-msvc  # ARM64

# 产物位置
# macOS:  src-tauri/target/release/bundle/dmg/
# Windows: src-tauri/target/release/bundle/nsis/
```

## 附录 B：版本发布流程

```bash
# 1. 更新版本号
#    - src-tauri/tauri.conf.json 中的 "version"
#    - src-tauri/Cargo.toml 中的 version

# 2. 本地验证
npm run tauri build

# 3. 打 tag 推送（触发 GitHub Actions 自动构建）
git add .
git commit -m "Release v0.1.0"
git tag v0.1.0
git push origin v0.1.0 --tags

# 4. 在 GitHub Releases 页面确认产物
```

---

*本文档基于对项目源代码的完整分析生成。macOS 打包可直接使用；Windows 打包需先完成第四章的代码适配。*