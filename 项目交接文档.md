# 🐱 像素猫 (Desktop Pet) — 项目交接文档

> 文档版本: 3.0  
> 生成日期: 2026-08-05  
> 项目类型: Tauri v2 桌面应用 (macOS)

---

## 一、项目概述

**像素猫** 是一个运行在 macOS 上的桌面宠物应用，基于 **Tauri 2 + Rust + Vanilla JS** 构建。应用在桌面上显示一只像素风格的小猫，监听全局键盘输入，每次按键小猫会播放一帧动画。支持窗口拖动、右键菜单、菜单栏 Tray 图标控制、番茄钟计时和喝水提醒等功能。

### 核心特性

| 功能 | 状态 | 说明 |
|------|------|------|
| 无边框透明窗口 (128×128px) | ✅ 已实现 | 紧凑尺寸只显示小猫 |
| 全屏覆盖（macOS 全屏应用之上可见） | ✅ 已实现 | 通过 `tauri-nspanel` 将 NSWindow swizzle 为 NSPanel |
| 桌面置顶（可切换） | ✅ 已实现 | 右键/Tray 菜单切换，状态同步 |
| 不显示 Dock 图标 (Accessory 模式) | ✅ 已实现 | `set_activation_policy(.Accessory)` |
| 不显示任务栏 (skipTaskbar) | ✅ 已实现 | `tauri.conf.json` 配置 |
| 窗口原生拖动 (startDragging) | ✅ 已实现 | Tauri 2 原生 API，无 JS 拖动 bug |
| 全局键盘监听 (keytap crate) | ✅ 已实现 | 需要 macOS 辅助功能权限 |
| 打字动画 | ✅ 已实现 | 待机为程序化绘制的像素猫，打字时切换左右按压 SVG，带呼吸/眨眼/睡觉状态 |
| 右键原生菜单 | ✅ 已实现 | 番茄钟、喝水提醒、置顶切换等 |
| 菜单栏 Tray 图标 | ✅ 已实现 | 左键显示宠物，右键打开菜单 |
| Toast 提示气泡 | ✅ 已实现 | 动画反馈、功能提示等 |
| **番茄钟** | ✅ 已实现 | 25 分钟专注 / 5 分钟休息，窗口动态扩展，已完成番茄数持久化显示 |
| **喝水提醒** | ✅ 已实现 | 每 45 分钟自动提醒，窗口扩展至 256×256 |
| **休息拉伸** | ✅ 已实现 | 每 1 小时自动提醒，右键菜单可手动触发 |
| **告诉我名字** | ✅ 已实现 | 输入框命名，名字持久化显示在宠物下方 |
| **开机自启** | ✅ 已实现 | 右键菜单「设置 → 开机自启」勾选，macOS LaunchAgent |

---

## 二、项目结构

```
pixelcat/                          # 项目根目录
├── package.json                   # Node.js 依赖配置
├── package-lock.json              # 依赖锁文件
├── .gitignore                     # Git 忽略规则
├── README.md                      # 项目说明（已更新）
│
├── scripts/
│   └── run-signed-dev.sh          # 开发模式 + 稳定签名
├── src/                           # ─── 前端 (Frontend) ───
│   ├── index.html                 # 入口页面（Tauri 加载此文件）
│   ├── main.js                    # 动画逻辑 & Tauri API 调用 & 番茄钟 & 喝水/拉伸提醒 & 名字
│   ├── comnyang-logo.svg          # 应用 Logo
│   ├── press-left.svg             # 打字动画帧（按压左）
│   └── press-right.svg            # 打字动画帧（按压右）
│
└── src-tauri/                     # ─── 后端 (Rust) ───
    ├── Cargo.toml                 # Rust 依赖配置
    ├── Cargo.lock                 # 依赖锁文件
    ├── build.rs                   # Tauri 构建脚本
    ├── .gitignore                 # Git 忽略规则
    ├── tauri.conf.json            # Tauri 核心配置（窗口、打包等）
    ├── capabilities/
    │   └── default.json           # 权限声明（Event、Window 拖动等）
    ├── src/
    │   ├── main.rs                # Rust 入口（调用 lib.rs 的 run()）
    │   └── lib.rs                 # 核心逻辑（窗口、NSPanel、菜单、Tray、键盘监听）
    ├── icons/                     # 应用图标（多尺寸）
    └── gen/                       # 自动生成，已 gitignore
```

---

## 三、技术栈

| 层 | 技术 | 版本要求 |
|----|------|----------|
| 框架 | Tauri 2 | ^2.11 |
| 后端语言 | Rust | ≥ 1.70 |
| 前端 | Vanilla JS + HTML5 Canvas | — |
| 运行时 | Node.js | ≥ 18 |

### 关键依赖

**Node.js (`package.json`):**

| 包 | 版本 | 用途 |
|---|------|------|
| `@tauri-apps/cli` | ^2.11.3 | Tauri CLI（构建/开发命令） |
| `@tauri-apps/api` | ^2.11.0 | Tauri 前端 API（事件、窗口操作） |
| `sharp` | ^0.35.2 | 图片处理（可能用于精灵图生成） |

**Rust (`Cargo.toml`):**

| 包 | 版本 | 用途 |
|---|------|------|
| `tauri` | 2 | Tauri 核心框架（含 `macos-private-api`、`tray-icon` 特性） |
| `tauri-plugin-opener` | 2 | 打开文件/链接插件 |
| `serde` / `serde_json` | 1 | 序列化/反序列化 |
| `keytap` | 0.4.0 | **全局键盘监听**（替代 `rdev`） |
| `objc2` | 0.6.4 | macOS 原生 API 调用 |
| `objc2-foundation` | 0.3.2 | macOS Foundation 框架绑定 |
| `objc2-app-kit` | 0.3.2 | macOS AppKit 框架绑定 |
| `tauri-nspanel` | v2.1 (git) | 将 NSWindow swizzle 为 NSPanel（全屏覆盖关键） |

---

## 四、核心架构详解

### 4.1 窗口配置 (`tauri.conf.json`)

```json
{
  "app": {
    "withGlobalTauri": true,       // 前端通过 window.__TAURI__ 访问 API
    "macOSPrivateApi": true,       // 允许使用 macOS 私有 API（NSPanel swizzle 需要）
    "windows": [{
      "label": "main",
      "width": 128,
      "height": 128,
      "resizable": false,          // 不可缩放
      "decorations": false,        // 无标题栏
      "transparent": true,         // 背景透明
      "shadow": false,             // 无窗口阴影
      "alwaysOnTop": true,         // 默认置顶
      "skipTaskbar": true,         // 不显示在 Dock
      "center": true               // 启动时居中
    }]
  },
  "build": {
    "frontendDist": "../src",      // 前端静态文件目录
    "beforeDevCommand": "",        // 无前端构建流程
    "beforeBuildCommand": ""
  }
}
```

### 4.2 权限声明 (`capabilities/default.json`)

授权的权限包括：
- `core:default` — 核心默认权限
- `core:event:default` — 事件系统
- `core:window:default` — 窗口操作
- `core:window:allow-start-dragging` — **原生窗口拖动**
- `core:window:allow-set-position` — 设置窗口位置
- `core:window:allow-outer-position` — 获取窗口位置
- `opener:default` — 打开外部链接

### 4.3 macOS 全屏覆盖机制 (关键难点)

为了让小猫窗口显示在所有**全屏应用**（如全屏播放视频）之上，普通 NSWindow 物理上无法做到。解决方式是将窗口在运行时 swizzle 成 NSPanel：

```rust
// lib.rs: NSPanel 子类定义
tauri_panel! {
    panel!(BasicPanel {
        config: {
            can_become_key_window: true,
            is_floating_panel: true
        }
    })
}
```

`make_panel_float_on_top()` 函数（`lib.rs` 第 45-66 行）执行以下操作：
1. `window.to_panel::<BasicPanel>()` — 将 NSWindow swizzle 为 NSPanel
2. `panel.set_level(PanelLevel::Floating)` — 设置为浮动层级
3. `panel.set_style_mask(nonactivating_panel)` — 不激活应用，不抢焦点
4. 通过 `msg_send!` 设置 `collectionBehavior` 的位掩码：
   - `1` (CanJoinAllSpaces) — 加入所有桌面 Space
   - `16` (Stationary) — 固定位置
   - `256` (FullScreenAuxiliary) — 允许在全屏 Space 显示
   - `1024` (CanJoinAllApplications) — 不参与台前调度 (Stage Manager)

**防御兜底**：后台线程每 2 秒重新应用一次 NSPanel 配置（`start_refresh_loop`），因为 macOS 在某些场景下可能重置 panel 层级。

### 4.4 动画逻辑 (`src/main.js`)

**动画资源：**
- **待机帧**：程序化绘制（`IDLE_SPRITE` 30×34 像素矩阵），支持呼吸、眨眼（每 3-7 秒随机）、睡觉（30 秒无操作）状态，睡觉时显示 "Z" 特效
- **打字帧**：`press-left.svg` / `press-right.svg` 两张 SVG 交替切换（Canvas 绘制，像素化渲染）
- **眼睛跟随**：Rust 端 `start_refresh_loop` 每 100ms 读取鼠标位置，通过 `cursor-position` 事件驱动瞳孔偏移

**状态机：**
- **idle**：程序化待机帧，等待键盘事件
- **blink**：每 3-7 秒随机眨眼 160ms
- **typing**：每次按键切换左右按压 SVG，360ms 后回到待机
- **sleep**：30 秒无操作进入睡觉状态，鼠标移动/按键唤醒

**事件触发链：**
```
用户键盘 ↓
keytap (Rust 全局监听) → emit("typing") → JS listen("typing") → handleTyping()
                                                  ↕ (兜底)
                                        document.addEventListener("keydown")
```

### 4.5 窗口拖动机制

使用 Tauri 2 原生 API `window.startDragging()`：

```js
canvas.addEventListener('pointerdown', (e) => {
  if (e.button === 0) win.startDragging();
});
```

优势：由操作系统接管拖动手势，不会出现 JS 拖动常见的「跳到角落」bug。

### 4.6 菜单系统

#### 右键菜单（`build_context_menu`）

| 菜单项 | ID | 功能 |
|--------|-----|------|
| 像素猫 vx.x.x | `title` | 标题（不可点击） |
| 喝水提醒 | `water_reminder` | **已实现**：立即触发喝水提醒 |
| 番茄钟 | `pomodoro` | **已实现**：打开番茄钟面板 |
| 休息拉伸 | `todo_stretch` | ✅ 已实现：立即触发拉伸提醒 |
| 设置 → 窗口置顶 | `pin` | 切换置顶 (CheckMenuItem)，与 Tray 菜单同步 |
| 设置 → 回到屏幕中央 | `center` | 窗口居中 |
| 设置 → 开机自启 | `todo_startup` | ✅ 已实现：勾选/取消开机自启 (CheckMenuItem) |
| 告诉我名字 | `todo_name` | ✅ 已实现：打开命名弹窗 |
| 隐藏宠物 | `hide` | 隐藏窗口 |
| 退出像素猫 | `quit` | 退出应用 |

#### Tray 菜单（`build_tray_menu`）

| 菜单项 | ID | 功能 |
|--------|-----|------|
| 像素猫 vx.x.x | `title` | 标题（不可点击） |
| 显示宠物 | `show` | 显示窗口 |
| 窗口置顶 | `pin` | 切换置顶 (CheckMenuItem)，与右键菜单同步 |
| 回到屏幕中央 | `center` | 窗口居中 |
| 退出像素猫 | `quit` | 退出应用 |

**注意**：右键菜单和 Tray 菜单的 `pin` 项共享 `AlwaysOnTop` 全局状态，通过 `sync_pin_checks()` 同步勾选状态（`lib.rs` 第 279-285 行）。Tray 的 `pin_item` 通过 `TrayPinItem` 状态持有引用。

### 4.7 全局键盘监听

使用 `keytap` crate 替代了早期版本中的 `rdev`。监听逻辑在后台线程运行（`lib.rs` 第 451-466 行）：

```rust
std::thread::spawn(move || {
    match Tap::new() {
        Ok(tap) => {
            for event in tap.iter() {
                if let EventKind::KeyDown(_) = event.kind {
                    let _ = handle.emit("typing", ());
                }
            }
        }
        Err(err) => {
            eprintln!("Failed to start global keyboard listener. Check macOS Accessibility permission: {err}");
        }
    }
});
```

**注意**：`keytap` 需要通过 macOS 辅助功能权限授权。启动失败时只会打印错误到 stderr，不会崩溃。

### 4.8 番茄钟系统 (新增功能)

番茄钟是**完全实现**的功能，并非占位菜单项。

**状态机** (`src/main.js` 第 94-191 行)：

```
idle → work → break → idle
           ↘ paused ↗
```

- **work**：25 分钟专注倒计时
- **break**：5 分钟休息倒计时
- **paused**：暂停状态，可恢复 work 或 break
- 每次完成一个 work session，`pomodoroTotalWork` 累计

**面板扩展机制**：通过 `setPanelExpanded()` 调用 Rust 命令 `set_panel_expanded`，窗口尺寸从 128×128（宠物模式）动态切换为 128×160（番茄钟模式）。关闭番茄钟后自动恢复。

**窗口尺寸常量**（`lib.rs` 第 22-27 行）：
```rust
const PET_SIZE: f64 = 128.0;
const POMODORO_WIDTH: f64 = 128.0;   // 宽度不变
const POMODORO_HEIGHT: f64 = 160.0;  // 高度增加
```

### 4.9 喝水提醒系统 (新增功能)

喝水提醒是**完全实现**的功能，并非占位菜单项。

- **自动触发**：程序启动后每 45 分钟自动提醒（`WATER_REMINDER_INTERVAL`）
- **手动触发**：通过右键菜单「喝水提醒」立即触发
- **窗口扩展**：提醒时窗口从 128×128 扩展为 256×256（`WATER_SIZE`），在窗口中央展示小猫
- **状态保存/恢复**：使用 `WindowSnapshot` 结构体保存扩展前的位置、尺寸和 pomodoro 展开状态，提醒结束后恢复（`lib.rs` 第 28-33 行）
- **提醒持续**：8 秒后自动恢复（`WATER_REMINDER_VISIBLE_MS` = 8000ms）
- **前端动画**：提醒期间添加 `water-active` CSS class，小猫尺寸过渡动画

```rust
const WATER_SIZE: f64 = 256.0;

struct WindowSnapshot {
    position: PhysicalPosition<i32>,
    size: PhysicalSize<u32>,
    pomodoro_expanded: bool,
}
```

### 4.10 Tray 图标交互

Tray 图标左键/右键行为（`lib.rs` 第 433-448 行）：
- **左键单击** → 显示宠物窗口（`window.show()` + `make_panel_float_on_top()`）
- **右键单击** → 弹出 Tray 菜单
- `show_menu_on_left_click(false)` 确保左键仅用于显示宠物

### 4.11 Rust 全局状态管理

项目使用 6 个全局状态（通过 `tauri::Builder::manage()` 注入）：

| 状态结构体 | 初始值 | 用途 |
|-----------|--------|------|
| `AppWindow` | `None` | 持有主窗口引用，供多个 command 使用 |
| `PanelExpanded` | `false` | 记录番茄钟面板是否展开 |
| `WaterRestoreState` | `None` | 喝水提醒前保存窗口状态，用于恢复 |
| `AlwaysOnTop` | `true` | 窗口置顶开关，菜单项共享 |
| `TrayPinItem` | `None` | Tray 菜单中 pin 的 CheckMenuItem 引用，用于同步勾选状态 |
| `StartupEnabled` | `startup_enabled()` | 开机自启开关，启动时检测 LaunchAgent 是否存在 |

### 4.12 Tauri Commands

| 命令 | 用途 | 调用方 |
|------|------|--------|
| `show_context_menu` | 弹出右键菜单 | JS 侧 `contextmenu` 事件 |
| `keep_on_top` | 重新应用 NSPanel 配置 | (预留) |
| `make_panel_key` | 让 panel 成为 key window，使 WebView 能接收鼠标点击 | JS 侧 |
| `set_panel_expanded` | 切换番茄钟面板的展开/折叠 | JS 侧番茄钟逻辑 |
| `focus_water_reminder` | 喝水提醒：保存当前状态，扩展窗口至 256×256 | JS 侧喝水提醒 |
| `restore_water_reminder` | 喝水提醒结束：恢复窗口状态 | JS 侧喝水提醒 |

---

## 五、文件清单 & 注意事项

### 5.1 核心文件（不可删）

| 文件 | 重要性 | 说明 |
|------|--------|------|
| `src/index.html` | ⭐⭐⭐ | 应用入口，Tauri 加载此页面 |
| `src/main.js` | ⭐⭐⭐ | 动画逻辑 + Tauri API 调用 + 番茄钟 + 喝水/拉伸提醒 + 名字 |
| `src-tauri/src/lib.rs` | ⭐⭐⭐ | Rust 核心逻辑（窗口、NSPanel、菜单、Tray、键盘监听、开机自启） |
| `src-tauri/src/main.rs` | ⭐⭐⭐ | Rust 入口 |
| `src-tauri/tauri.conf.json` | ⭐⭐⭐ | Tauri 配置 |
| `src-tauri/Cargo.toml` | ⭐⭐⭐ | Rust 依赖 |
| `src-tauri/capabilities/default.json` | ⭐⭐⭐ | 权限声明 |
| `package.json` | ⭐⭐⭐ | Node 依赖 + 构建脚本 |
| `src/press-left.svg` / `press-right.svg` | ⭐⭐⭐ | 打字动画资源（缺此打字动画无显示） |

### 5.2 非核心文件（可清理）

| 文件 | 状态 | 建议 |
|------|------|------|
| （无） | — | 遗留文件已在 v3.0 清理完毕 |

### 5.3 文件变更追踪（v1.0 → v2.0）

| 文件 | 变更说明 |
|------|----------|
| `README.md` | **已重写** 更新了功能清单、精灵图规格、菜单说明 |
| `src/main.js` | 新增番茄钟状态机、喝水提醒逻辑、`showWaterReminder()`、`invoke()` 封装 |
| `src/index.html` | 新增番茄钟显示面板 `#pomodoro-display`、`water-active` / `pomodoro-active` CSS 样式 |
| `src-tauri/src/lib.rs` | 新增 `PanelExpanded`、`WaterRestoreState`、`WindowSnapshot` 状态；新增 `focus_water_reminder`、`restore_water_reminder`、`set_panel_expanded`、`make_panel_key` 命令；菜单项 `todo_reminder` → `water_reminder`、`todo_pomodoro` → `pomodoro`；Tray 左键点击显示宠物 |

---

## 六、开发指南

### 6.1 环境要求

```bash
# 检查版本
node --version   # ≥ 18
rustc --version  # ≥ 1.70
cargo --version
```

### 6.2 本地运行

```bash
# 1. 安装前端依赖
npm install

# 2. 开发模式运行（推荐，带稳定签名）
npm run dev:signed

# 3. 或直接 Tauri 开发模式（可不用稳定签名）
npm run tauri dev

# 4. 构建发布版本
npm run tauri build
```

### 6.3 macOS 权限设置

`keytap` 需要辅助功能权限：

1. 打开 **系统设置 → 隐私与安全性 → 辅助功能**
2. 点击锁图标解锁
3. 点击 `+` 添加应用（在 `src-tauri/target/debug/` 或 `release/` 中找到编译后的应用）
4. 勾选启用

### 6.4 可用 npm scripts

```bash
npm run tauri dev     # 开发模式
npm run tauri build   # 发布构建
npm run tauri         # 查看所有 Tauri CLI 命令
```

---

## 七、核心业务流程

### 7.1 番茄钟完整流程

```
用户点击「番茄钟」菜单项
    ↓
Rust: menu_event_handler → emit("open-pomodoro") + set_window_panel_mode(true)
    ↓
JS: listen("open-pomodoro") → togglePomodoro()
    ↓
JS: startPomodoro() → setPanelExpanded(true) → invoke("set_panel_expanded")
    ↓
Rust: set_window_panel_mode() 窗口从 128×128 扩展为 128×160
    ↓
setInterval(tick, 1000) 每秒更新倒计时
    ↓
倒计时归零 → work→break 或 break→idle
    ↓
idle 时 → setPanelExpanded(false) → 窗口恢复 128×128
```

### 7.2 喝水提醒完整流程

```
自动：startWaterReminder() 设置每45分钟定时器
手动：右键菜单「喝水提醒」
    ↓
showWaterReminder()
    ↓
invoke("focus_water_reminder")  // Rust 保存当前窗口状态并扩展为 256×256
    ↓
document.body.classList.add('water-active')  // CSS 过渡动画
    ↓
showToast("该喝水啦，起来接一杯水吧")
    ↓
8秒后 setTimeout
    ↓
invoke("restore_water_reminder")  // Rust 恢复窗口位置、尺寸、pomodoro 状态
document.body.classList.remove('water-active')
```

---

## 八、常见问题 FAQ

### Q1: 运行后看不到小猫窗口？

- 窗口 128×128 很小，仔细在桌面中央寻找
- 检查终端是否有编译错误
- 尝试 `Cmd+Tab` 切换应用确认程序是否在运行
- 左键点击菜单栏 Tray 图标可显示宠物

### Q2: 键盘触发没反应（动画不动）？

- `keytap` 需要 **辅助功能权限**（见 6.3 节）
- 作为兜底，应用内键盘事件也支持（在窗口内打字会触发）
- 检查终端是否有 `Failed to start global keyboard listener` 错误

### Q3: 全屏看视频时小猫不显示？

- 确认 `macOSPrivateApi: true` 已启用
- NSPanel swizzle 机制依赖 `tauri-nspanel` crate（已配置在 Cargo.toml）
- 后台刷新线程每 2 秒会重新应用配置

### Q4: 如何修改猫咪外观？

- **打字动画**：替换 `src/press-left.svg` / `src/press-right.svg`
- **待机动画**：修改 `src/main.js` 中的 `IDLE_SPRITE` 像素矩阵（30×34 格，0=透明，1=黑色，2=白色描边，5/6/8=眼睛/瞳孔）
- Canvas 尺寸常量在 `src/main.js` 顶部（`FRAME_W`、`FRAME_H`）

### Q5: 构建发布版本需要注意什么？

- `npm run tauri build` 会在 `src-tauri/target/release/bundle/` 生成 `.dmg` 安装包
- 发布前更新 `tauri.conf.json` 中的 `version`
- 确保所有图标文件齐全

### Q6: 番茄钟面板不显示或窗口大小异常？

- 确认 `set_window_panel_mode()` 中的尺寸常量正确（PET_SIZE=128, POMODORO_HEIGHT=160）
- 检查 `PanelExpanded` 状态是否同步

### Q7: 喝水提醒后窗口位置不对？

- 确认 `WaterRestoreState` 正确保存和恢复了 `WindowSnapshot`
- 检查 `focus_water_reminder` 和 `restore_water_reminder` 的调用顺序

---

## 九、后续开发建议

### 待实现功能 (TODO)

| 功能 | 说明 |
|------|------|
| 番茄钟自定义时长 | 当前固定 25/5 分钟 |
| 喝水提醒配置 | 间隔时长自定义、提醒时段设置 |
| 多语言支持 | 当前仅中文菜单 |
| Windows 兼容性 | `keytap` 和 `tauri-nspanel` 均为 macOS 专用 |

### 架构改进建议

1. **番茄钟增强** — 可增加自定义时长、通知中心推送
2. **喝水提醒配置** — 可增加间隔时长自定义、提醒时段设置
3. **动画逻辑增强** — 可增加更多动作状态（如循环动画、更多待机动作）
4. **多语言支持** — 当前仅中文菜单，可考虑国际化
5. **Windows 兼容性** — 当前 `keytap` 和 `tauri-nspanel` 均为 macOS 专用，Windows 上需替代方案

---

## 十、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-07-02 | 初始交接文档（功能占位阶段） |
| 2.0 | 2026-07-03 | 更新文档：番茄钟、喝水提醒已实现，新增窗口扩展机制、Rust 状态管理、Tray 交互等 |
| 3.0 | 2026-08-05 | 新增休息拉伸、告诉我名字、开机自启；番茄钟计数持久化；清理遗留文件；动画改为 SVG + 程序化绘制 |

---

*本文档基于对项目源代码的完整分析生成。如有疑问请查阅对应源代码文件。*